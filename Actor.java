package blockBreaker;

import java.awt.Graphics2D;

public interface Actor
{
    /**
     * Draws this Actor on the graphics object.
     * 
     * @param g the Graphics2D object on which to draw.
     */
    public void draw( Graphics2D g );
}
